# 2026-06-12 工作日志

## C&I 产品页产品展示区优化

- 将原来的 `Battery` 图标占位改成 `ProductShowcase` 组件
- 组件逻辑：优先加载图片 → 图片 404 则尝试视频 → 都失败则显示图标占位
- 图片路径：`/images/products/ci-battery-cabinets.jpg`
- 视频路径：`/videos/ci-product.mp4`
- 组件支持自动播放、循环、静音（符合 B2B 网站最佳实践）
- `next build` 成功，`/products/ci-battery-cabinets` 9.04 kB

## 视频播放修复

- 问题：浏览器自动播放策略阻止 autoPlay，视频卡住不动
- 修复：改为点击播放模式，显示视频第一帧 + 白色播放按钮覆盖层
- 用户点击播放按钮后调用 `video.play()`，播放后按钮消失

## GitHub Push Protection 问题修复

- 原因：`scripts/run-migration.py` 第6行硬编码了 `sbp_` token，GitHub secret scanning 拒绝 push
- 修复：将 token 改为从环境变量 `SUPABASE_LOGIN_TOKEN` 读取
- 使用 `git filter-branch` 从整个 git 历史中清除 token
- 清理了 refs/original、reflog、并 gc --prune=now
- 验证：`git log --all -p | grep sbp_a3b06` 返回空（exit 1）
- `.env.local` 里有 token 但已被 `.gitignore` 排除，不会提交
- push 时需要 `git push --force origin main`（历史被重写）

## Vercel 环境变量配置完成

- 用户在 Vercel Dashboard 添加了以下环境变量：
  - `ADMIN_USER` → Production + Preview
  - `ADMIN_PASS` → Production + Preview
  - `SUPABASE_LOGIN_TOKEN` → Production + Preview
- 同时启用了 System Environment Variables

## 移除 API 路由中的硬编码 token fallback

- 两个管理后台 API 路由原本有硬编码的 `sbp_` token 作为 fallback：
  - `src/app/api/admin/submissions/route.ts`
  - `src/app/api/admin/downloads/route.ts`
- 由于 Vercel 环境变量已配置，硬编码 fallback 不再需要
- 移除了 fallback，改为纯 `process.env.SUPABASE_LOGIN_TOKEN`
- 提交 `d520e77` 成功推送到 GitHub，无 Push Protection 拦截
- Vercel 将自动部署，部署后管理后台通过环境变量访问 Supabase

## 第二次 git filter-branch 清除历史 token

- 原因：虽然最新代码已删除 fallback，但旧 commit 91696f4 历史中仍有 sbp_ token，GitHub Push Protection 扫描全历史
- 再次执行 git filter-branch --tree-filter 把所有历史中的 sbp_ 替换为 REMOVED_TOKEN
- 清理 refs/original（需 git update-ref -d）、reflog、gc --prune=now --aggressive
- 验证：git log --all -p | grep sbp_ 返回 0 匹配
- git push --force origin main 成功，不再被拦截

## 产品页联系按钮锚点修复

- 问题：点击导航栏"联系"按钮（href="#contact"）在产品页不起作用
  - Energy Storage System 页面完全没有 contact section
  - C&I Battery Cabinets 页面有 ContactForm 但没有 id="contact" 锚点
- 修复：
  - energy-storage-system/page.tsx：添加 ContactForm import + id="contact" 的 CTA section
  - ci-battery-cabinets/page.tsx：在 contact section 添加 id="contact" + scroll-mt-20
- next build 通过

## SEO 博客系统上线

- 创建博客系统：/blog 列表页 + /blog/[slug] 详情页
- 文章数据集中在 src/app/blog/blog-data.ts 的 ARTICLES 数组
- 每篇文章中英双语，包含 slug/date/readTime/category/tags/title/description/body
- 文章详情页功能：目录导航（桌面侧栏）、Markdown 渲染（标题/列表/表格/加粗）、相关文章推荐、ContactForm CTA
- 导航栏添加 "Blog" / "技术博客" 链接
- 页脚从 4 栏改为 5 栏，新增 "Resources" 列指向博客文章
- 5 篇初始 SEO 文章：
  1. commercial-battery-storage-roi — 投资回报分析
  2. ci-solar-storage-installation-guide — 安装指南
  3. lithium-battery-cabinet-vs-rack-mount — 产品对比
  4. solar-storage-certification-guide — 认证解读
  5. how-to-choose-energy-storage-supplier — 采购指南
- Next.js 15 params 必须用 Promise + use() 模式
- next build 成功，git push 成功

## 每日博客写作自动化

- 创建了定时任务 "SEO博客文章日更"（ID: automation-1781254485925）
- 状态：PAUSED（需要用户手动激活）
- 频率：每天早上 9:00
- 功能：自动读取已有文章 → 选择新关键词 → 写文章 → 添加到 blog-data.ts → git commit + push
- 提供了 20 个候选关键词方向
